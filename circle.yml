version: 2.1
machine:
  java:
    version: oraclejdk8
dependencies:
  pre: 
    - export TERM="dumb"; if [ ! -e .gradle ]; then gradle wrapper ; else echo "Gradle installed" ;fi
  override:
    - ./gradlew clean build
  cache_directories:
    - ".gradle"
deployment:
  dev:
    branch: develop
    heroku:
      appname: prady-hello
