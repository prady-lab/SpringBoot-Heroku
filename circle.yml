machine:
  java:
    version: oraclejdk8
dependencies:
  pre: 
    - export TERM="dumb"; if [ ! -e .gradle ]; then gradle wrapper ; else echo "Gradle installed" ;fi
  override:
    - ./gradlew clean build
  cache_directories:
	- ".gradle"
deployment:
  staging:
    branch: develop
    heroku:
      appname: prady-hello
