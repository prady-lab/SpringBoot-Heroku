machine:
  java:
    version: oraclejdk8
dependencies:
  pre: 
    - export TERM="dumb"; if [ ! -e .gradle ]; then gradle wrapper ; else echo "Gradle installed" ;fi
  override:
    - ./gradlew clean build
deployment:
  staging:
    branch: develop
    heroku:
      appname: prady-hello
